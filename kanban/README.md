# Kanban

Single-board Kanban MVP. See [AGENTS.md](AGENTS.md) for requirements.

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:3000

Tests: `npm test` and `npm run test:e2e` (from `frontend/`).
